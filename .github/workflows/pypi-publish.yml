name: Publish Python 🐍 distributions 📦 to PyPI and TestPyPI

on:
  push:
    branches:
      - main  # Set this to the branch containing the workflow file
    tags:
      - 'v*'  # This will match tags like v1.0, v20.15.10

jobs:
  build-n-publish:
    name: Build and publish Python 🐍 distributions 📦 to PyPI and TestPyPI
    runs-on: ubuntu-latest
    permissions:
      id-token: write
    steps:
    - uses: actions/checkout@v2
    - name: Set up Python 3.8
      uses: actions/setup-python@v2
      with:
        python-version: 3.8
    - name: Install pypa/build
      run: >-
        python -m
        pip install
        build
        --user
    - name: Build a binary wheel and a source tarball
      run: >-
        python -m
        build
        --sdist
        --wheel
        --outdir dist/
        .
    - name: Publish distribution 📦 to Test PyPI
      uses: pypa/gh-action-pypi-publish@release/v1
      with:
        password: ${{ secrets.TEST_PYPI_API_TOKEN }}
        repository-url: https://test.pypi.org/legacy/
    - name: Publish distribution 📦 to PyPI
      if: startsWith(github.ref, 'refs/tags')
      uses: pypa/gh-action-pypi-publish@release/v1
      with:
        password: pypi-AgEIcHlwaS5vcmcCJGI1Y2I1YWU4LTNhYmUtNDdiMy05NWFjLWJmYTA3MDE2ZTg3ZgACKlszLCIxMzg5Zjc1MS0wNDQyLTRlMjUtYTdlNi1hYjRlZmQzY2VlMDAiXQAABiCmUcvX-TVloisV483dBbVV_vVu45eDkQQcPxsIArxzJg
